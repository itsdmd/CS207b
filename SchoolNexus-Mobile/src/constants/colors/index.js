// https://colorhunt.co
const bg = "#222831"; // Background
const secondary = "#393E46"; // Dark Grey (secondary button)
const primary = "#00ADB5"; // Cyan (highlight)
const text_primary = "#EEEEEE"; // White (text)
const text_secondary = "#AAAAAA"; // Bright Gray (secondary text)
const error = "#B06161"; // Red (error, cancel)

export default {
	bg,
	secondary,
	primary,
	text_primary,
	text_secondary,
	error,
};
